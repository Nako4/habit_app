<h1>Users#show</h1>

<div class="whole">
  <div class="calender">
    <div>top</div>
  </div>
  <div class="habit-list">
    <div>left</div>
    <% @habits.each do |habit| %>
      <div class="action" data-id=<%= habit.id %> data-check=<%= habit.checked %>>
        <%= "#{habit.action_id}" %>
      </div>
    <% end %>


  </div>
  <div class="achieve">
    <div>right</div>
  </div>
</div>
<p>Find me in app/views/users/show.html.erb</p>
<p>My Page<%= current_user.id %></p>

<p>Find me in app/views/users/show.html.erb</p>
